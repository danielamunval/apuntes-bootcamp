package J3_Condicionales;

public class C2_IF_otro {
    public static void main(String[] args) {
        boolean esVerdadero = true;

        if(esVerdadero) System.out.println("Verdadero");
        // esta es una forma de registrar en casos de variables únicas. La estructura es más lineal, se puede considerar para optimizar código, pero se ve MUY poco.
        // pero se ve más desordenado; es probable que no se entienda bien. No está TAN ordenado, lo más probable es que no se entienda si lo ve otra persona.
        // lo ideal es no escribirlo así. :D

    }
}
