package C20_300625;

public class Apuntes {
    /*Abstracción (notion)

    * Interfaces (notion) -> nacen porque no se pueden hacer herencias de las herencias (multiherencias). Sin embargo, con las interfaces se plantea una solución con el metodo "implements". Si bien las multiherencias no se pueden realizar, si se pueden realizar interfaces sobre interfaces.

    * Las interfaces están en otro package, por lo tanto se pueden sobre escribir cuantas veces sea necesario.

     */
}
